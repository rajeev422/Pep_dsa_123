class Solution {
public:
    bool rotateString(string s, string goal) {
        int n=s.size();
        if(s.size()!=goal.size())return false;
        string neww=s+s;
        int j=0;
        for(int i=0;i<2*n;i++){
            if
        }
    }
};